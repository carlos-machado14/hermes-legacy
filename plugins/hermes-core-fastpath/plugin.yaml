name: hermes-core-fastpath
version: 1.7.0
description: Intercepta mensagens normais do gateway, aplica SLA por complexidade e devolve respostas pelo event loop original, preservando comandos slash.
provides_hooks:
  - pre_gateway_dispatch
